# This package uses pnpm as its package manager.
# npm and yarn users can still install and use the published package normally.
# Only contributors working on this repo need pnpm.
shamefully-hoist=false
