# Build output
dist/
*.tsbuildinfo

# Dependencies
node_modules/
package-lock.json
yarn.lock

# Coverage
coverage/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# Claude Code (local-only settings)
.claude/settings.local.json

# Generated API docs (typedoc output) — ignore directory contents, not the directory itself,
# so hand-written docs at docs/<file>.md can be explicitly re-included below.
docs/*

# Hand-written docs to track
!docs/spec-compliance.md
!docs/proposals/
!docs/proposals/*.md

# Fetched testdata (from xx.hocon)
tests/lightbend/testdata/expected/
.xx-hocon-version

# Deferred-resolution fixtures fetched from xx.hocon via `make testdata`
tests/lightbend/testdata/hocon/deferred-resolution/
